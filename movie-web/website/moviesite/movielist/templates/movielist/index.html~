{% load static %}
<html>
  <head>
    <meta charset="utf-8">
    <meta name="author" content="LanderX">
    <title>Emitm电影网:让生活遇见电影</title>
  </head>
  <body>
    <h1><center>电影列表</center></h1>

    {% if movies %}
    <ul>
      {% for movie_title in movies %}
      <li><a href="/{{ movie_title }}"><img src="/static/movielist/{{movie_title}}.png"
                                            alt="暂无图片"></a></li>
      <li><a href="/{{ movie_title }}">{{ movie_title }}</a></li>
      {% endfor %}
    </ul>
    {% endif %}

    <h2><center>页面跳转</center></h2>
    <p><center>
      <a href="/1">第一页</a>
      ...
      {% for page in pages %}
      <a href="/{{page}}">{{page}}</a>
      {% endfor %}
      ...
      <a href="/51">尾页</a>
    </center></p>
    
    <h1><center>end</center></h1>
  </body>
</html>
